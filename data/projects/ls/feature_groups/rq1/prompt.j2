You are assisting a BFT/PFT design mapping (RQ1).
Task: extract ONLY the module/design fields from the provided PDF.

Return a SINGLE JSON object:
{
  "<field_name>": {"value": <typed or "NA">, "evidence": "<verbatim or NA>", "location": "<page/figure/section or NA>"}
}

STRICT RULES:
- Do NOT infer beyond the text. If not explicitly present, set value="NA", evidence/location="NA".
- Enums must use the allowed tokens exactly.
- Lists must be semicolon-separated strings (e.g., "PBFT;HotStuff").
- Booleans must be true/false.

ENUM HINTS:
- algorithm_family: PBFT; Tendermint; HotStuff; DAG-based; other
- module.commit_rule: 3-chain; 2-chain+TC; Tendermint-style; DAG-ordered; other
- module.view_change: linear; quadratic; optimistic; other
- module.leader_selection: round-robin; VRF/lottery; stake-weighted; other
- module.batching_policy: fixed; adaptive; none
- module.data_availability: none; erasure-coding; DA-committee; other
- module.sig_scheme: Ed25519; BLS12-381; secp256k1; other

FIELDS:
{{ field_list|join(", ") }}

CITATION (for context only):
{{ citation_header }}

HINTS FOR EVIDENCE (if present):
- family/lineage names near “builds on”, “extends”, “variant of”
- commit rule: 3-chain / 2-chain + TC / Tendermint-style
- view-change policy: linear/quadratic/optimistic
- leader: rotation/selection/VRF/stake
- batching/pipeline/fast path/DAG/DA/threshold/aggregate signatures

---- BEGIN PDF with [Page N] ----
{{ pdf_text }}
---- END PDF ----
